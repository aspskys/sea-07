---
name: starry-cli-user-list
description: >-
  使用 big_data user_list 按 st_role_id、st_user_id、st_distinct_id、角色 ID、
  用户 ID、账号 ID、玩家 ID、设备标识或不确定类型的用户标识定位 Starry/星云 用户。
---

# User List

文中的 `starry-cli ...` 是语义命令，实际执行用 wrapper 并原样传参.

```bash
starry-cli big_data user_list <field_value>
starry-cli big_data user_list --field_name st_user_id --field_value <value>
```
